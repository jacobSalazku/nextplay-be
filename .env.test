# Test-only environment. No secrets — these credentials belong to the
# throwaway Docker Postgres in docker-compose.test.yml.
# Loaded ONLY by the Jest setup; the real .env / .env.local are never read
# during tests.
DATABASE_URL="postgresql://nextplay:nextplay@localhost:5433/nextplay_test"
